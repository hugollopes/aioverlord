apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f dev.yml
    kompose.version: 1.9.0 (093c756)
  creationTimestamp: null
  labels:
    io.kompose.service: web
  name: web
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.service: web
    spec:
      containers:
      - args:
        - npm
        - run
        - e2e
        image: localhost:5001/frontend:latest
        name: web
        ports:
        - containerPort: 8080
        resources: {}
        volumeMounts:
          - name: code
            mountPath: /app
      initContainers:
      - name: flaskprobe
        image: localhost:5001/aliveprobe:latest
        command: ['sh', '-c','until (curl -s "http://flask:5000/" --max-time 1 | grep "db connection active") ;
                  do echo waiting for flask server; sleep 2; done;']
      restartPolicy: Always
      volumes:
        - name: code
          hostPath:
            path: /home/hugo/PycharmProjects/AppProject/aioverlord-frontend
status: {}
